#!/bin/bash

FILE=$1
if [ -z "$FILE" ]; then
	files=(~/Pictures/wallpapers/wallpapers/*)
	xwallpaper --zoom "${files[RANDOM % ${#files[@]}]}"
else
	xwallpaper --zoom ~/Pictures/wallpapers/wallpapers/"$FILE"
fi
