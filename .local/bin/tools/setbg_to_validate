#!/bin/bash

FILE=$1
if [ -z "$FILE" ]; then
	files=(~/Pictures/wallpapers/wallpapers_to_validate/*)
	xwallpaper --zoom "${files[RANDOM % ${#files[@]}]}"
else
	xwallpaper --zoom ~/Pictures/wallpapers/wallpapers_to_validate/"$FILE"
fi
